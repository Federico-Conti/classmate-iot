{
  "name": "classmate-database-tools",
  "private": true,
  "scripts": {
    "migrate": "node scripts/migrate.mjs --database ../infra/data/classmate.db",
    "provision": "node scripts/provision.mjs --database ../infra/data/classmate.db --seed seeds/poc.example.json",
    "test": "node --test tests/*.test.mjs"
  }
}
